import { Layout } from '@/components/Layout';
import { PageHeader } from '@/components/PageHeader';

export function Emergency() {
  return (
    <Layout>
      <PageHeader title="Emergencia" backTo="/" />
    </Layout>
  );
}
